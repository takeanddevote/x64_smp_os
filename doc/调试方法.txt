1、在移位操作中，类型强转的重要性：
    比如定义一个数据类型为：u32 val = 0x1234，如果你想把它拓展为u64的高位：val << 32，编译为汇编时：shr eax，32，这就有问题了，因为eax是32位的，
左移32位后，结果eax为0，即val << 32左移后的结果为0。因此需要：(u64)val << 32，编译为汇编时：shr rax，32，此时结果就正确了。
    当然这应该跟编译器有关，因此需要注意这点。
